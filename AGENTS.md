# 项目代理规则

这是一个个人项目。只保证文档化需求功能的正常使用和常见操作失误。项目边界规则（严
重度 P0/P1/P2/P3 定义、对抗性输入/恶意编辑/手工改写状态/权限或不可变文件失败/不
受支持平台等只作 P3 建议且不阻塞 PASS、复现路径与阻塞项证据规则、QA 用例设计与执
行边界）唯一持有在 `prompts/reviewer-base.md`，本文件不重复其全文。范围外的发现项
仅作建议；如果不存在范围内的阻塞项，结果必须为 PASS。

在每一次子代理派发中都明确重申这条边界；不要指望子代理自己去另一份文档里发现它。
在报告某项改动无法推送之前，主代理必须独立验证所声称的正常使用复现路径。

绝不催促或施压审查子代理。不要让它停止阅读或检查、提前收敛、只使用已收集的证据、
立即返回，或以其他方式缩短分配给它的审查。状态询问只能询问进度，并且必须明确告诉
子代理继续，直到每一项分配的检查全部完成。在不当施压之后返回的判定无效，必须丢弃
并干净重跑，且不计入一次已完成的审查-修复循环。
