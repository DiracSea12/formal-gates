---
name: formal-gates
description: 用于每一次涉及项目内容的创建、编辑、移动或删除请求（无论仓库、产品、文件类型或规模）；也用于用户明确要求的 formal-gates 对齐、审查、发布、Seal、安装或诊断。不涉及修改的提问、解释、诊断和 review 不触发，除非用户要求正式执行。
---

# Formal Gates

以 VCS 为依托的开发质量管理工作流：CLI 只保存一个临时状态文件，host 负责启动各个独立
代理，所有快照和 diff 都由仓库原生 VCS 持有。

## 范围边界

只保证文档化需求功能的正常使用和常见操作失误。除非用户明确要求加固，对抗性输入、恶意
编辑、手工改写状态、权限或不可变文件失败、不受支持的平台，以及其他人为构造的攻击或违
规，都只作 P2 建议，不能阻塞 PASS，也不能引出新机制、兼容路径或测试。

每个阻塞性发现项都必须能从文档化的公开入口出发，用正常用户操作或一个常见失误复现。确
定性规则在拥有它的最低层测试；不要添加主要目的是重测另一个测试或校验器的测试。

正式 PASS 的唯一来源是独立代理通过 CLI 返回的已校验结论；若独立代理不可用，把该审查报告为受阻。

## 开发受理流程

所有创建、编辑、移动或删除项目内容的请求都要走本受理流程；不涉及修改的需求不进入，除
非用户明确要求走正式流程。

在写入任何文件或派发子代理之前，主代理必须：

1. 检视工作区，确定自己能直接判定的事实。
2. 拷问并对齐所有有实质影响的细节，不留任何需要主代理猜测或假设的余地。逐维拷问：目
   标动机、用户场景、范围边界/非目标、细节与默认值、异常与失败路径、验收标准、约束
   与取舍、风险；记录拷问轨迹与"考虑过但放过"项（放过须有理由且不构成实质猜测）；返
   回 PASS 前自检"是否还有任何有实质影响的细节是我不得不猜的"。仍然一次只问一个有实
   质影响的决策，用日常语言讲清背景与选项，需求细节和主要技术方案都算；次要实现细节
   自行决定。“提出方案”、给出推荐或说明默认选择，都不等于需求已经澄清并确认。
3. 在需求澄清完成后，呈现完整整合后的需求与技术方案，单独发起确认提问并等待用户明确
   确认。即使第 2 步没有待澄清问题，也必须显式说明“无待澄清问题”，再发起这次确认；
   不得省略、隐含或把确认混在路线选择中。只有用户明确确认完整需求与方案，需求澄清才
   算完成。
4. 只有在用户明确确认完整需求与方案后，才评估工作量——规模、耦合、风险和验证复杂度，查询当前所有已存在的门（
   `formal-gates package route-candidates --root <package>`），然后呈现开发流程选择
   ：lightweight（不创建正式流程）、`full`（QA 加全部已发现门）、`custom`（展示 QA
   和完整已发现门列表供用户任意组合，可不选 QA）。可以给出推荐并解释理由。

需求确认与路线选择不得合并。路线选择只能在需求与方案明确确认后单独询问，且路线只问
一次；路线选择本身不构成需求确认。用户未明确确认前，保持在受理阶段，不得写入文件或
派发子代理。

用户在开发中途追加新需求时，暂停受影响的写入，先澄清并对齐该需求，重新给出一份完整方
案摘要并取得明确确认；已选路线保持不变，除非用户明确要求重选。

选择 `full` 或 `custom` 时，开发之前要把完整的已确认需求与技术方案持久化到环境中任何
一种稳定的文档格式里（不要求特定格式或插件），然后进入后面的正式流程。本阶段的澄清用
于决定需求细节和技术方案；正式流程第 2 步会按 `requirements-clarification` 动作把结
果正式登记进开发流程。需求整体无法作为一个连贯有界单元来实现和验证时，先读
`references/sliced-runs.md` 再拆分。

`lightweight` 就是普通的 vibe coding 流程，不创建流程状态或正式任务切片，PRD、
OpenSpec 等任何设计和需求文档格式都可以省略；但用户如果要用，依然可以正常创建和使用
这些文档。

## 正式流程

下面九步的顺序只由 `SKILL.md` 定义。进入本流程时，路线（`full` 或 `custom`）在受理阶
段已经定下：custom 未选中的审查阶段就此省略，Seal 期间不会补做。每一步的确切命令形式
见 `references/formal-flow.md`。

1. **启动。** 选一个项目的外部 VCS，冻结它的原生基线标识，运行 `workflow start`，登
   记主需求和所有对应的需求或方案文档。不支持无 VCS 的正式开发流程。
2. **需求澄清登记。** 只登记受理阶段已对齐的需求并记录 PASS（`requirement --confirmed`
   绑定）。澄清问答、呈现、确认、把对齐需求写入需求文件，都在受理阶段完成。需求变更：
   QA 设计基于此前用例重做完整集（未受影响保持 PASS）；meaning-preserved 重绑定（主代
   理认定 + 用户确认）保留 PASS 并重绑快照；语义已变回需求澄清。
3. **绑定已选路线。** 根据 `workflow route-candidates` 核对绑定的目录，记录已选好的
   路线，不再重复询问。custom 省略的部分获得路线跳过授权；后续新增需要用户明确指示；
   开发开始之后不能再加入 QA。
4. **正式开发之前（两段式检查）。** 先派发 Part 1 产品审（`product-review`），用独立
   零上下文审查者审受理阶段已实例化并确认的需求文档：需求本身是否合理、需求细节是否
   合理。每个发现项由主代理转达给用户逐项拍板——用户接受则视为通过、不阻塞，未接受则
   按用户指示修订需求/方案后重新产品审。产品审不产生终态 FAIL，需求是否成立由用户决
   定。Part 1 全部通过后，Part 2 技术审（`start-readiness`）与 QA 用例设计
   （`qa-design`）并行推进，QA 用例设计通过后派发独立 `qa-review`。完整候选用例集只
   通过 `workflow qa-design` 记录，每套用例都包含 STATIC（静态测试）和 LIVE（实际执
   行测试）。`qa-review` 用新的零上下文审查者审阅用例集，每个待定用例的结论：PASS 代
   表批准用例并解锁开发，FAIL 代表退回设计，RUNTIME_ERROR 只重试审查。这个循环不消
   耗开发后的审查轮次。
5. **开发。** 运行 `development-worker` 准备命令，记录开发开始并锁定路线。任务中断后
   可以继续同一个 PREPARED 任务，只是重新组装提示词，已记录的开始边界不变。开发子代
   理与审查者完全隔离，任务内容见『独立派发』。子代理只做已确认范围内的工作：新增或跟
   踪每条交付路径时，先把它加入 VCS，返回前核查基线到当前的完整 diff。
6. **固定当前快照。** 实现完成后，用原生 VCS 创建一个不可变标识，然后运行
   `workflow snapshot` 记录它。后续审查只能用这个快照，不能用工作树的当前状态。
7. **开发后审查。** 并行派出 QA 审查和各个门的审查。QA 只用已批准的测试用例；每个门
   只看基线到当前的完整 diff，各自独立评审，互不干扰。审查代理不直接写流程状态，由主
   代理（编排者）校验后统一记录。结果一旦记录为 PASS 或 FAIL，对该快照即为最终结论；
   只有 PENDING 和 RUNTIME_ERROR 可以重跑。
8. **修复。** 出现 QA FAIL 或 P0/P1 门发现项时，整个轮次退回修复，此时同轮的 P2 一并
   处理。开始编辑前先冻结当前 VCS 标识。总任务实例把集成发现项分发给各子任务实例，收
   到各子任务已 Seal 的修复后直接调用 `workflow snapshot`；其他子任务实例准备并派发
   开发工作者，完成后冻结新标识并调用 `workflow snapshot`。主代理通过修复前紧邻快照
   到当前的比较检查修复内容；派发独立门审/QA 前，先用目标项目自己的构建/测试做便宜
   健全性检查，明显失败如编译不过直接退回修复、不派发子代理，无校验入口则跳过。

   **继承判定：** 如果修复范围不影响任何此前已通过的被选验证，主代理可以直接做继承判
   定（写明理由，继承所有此前 PASS，包括 QA）；否则，对每个此前通过的被选门单独派发
   继承判定并重跑 QA。涉及共享 API、公开行为、配置、依赖、跨门职责或因果不确定的修复
   ，一律用独立继承判定。只重跑标记为 `RERUN` 的门，给它完整的基线到当前比较。
9. **Seal。** 汇总前后各确认一次当前 VCS 标识。PENDING 结果直接阻塞 Seal；
   RUNTIME_ERROR 需要用户手动跳过；QA FAIL 或 P0/P1 必须先修复，直到共享上限耗尽或用户主动要求才可
   授权跳过。路线跳过和 Seal 跳过都会记录在摘要里。已授权的 Seal 跳过在当前快照仍被
   其他结果阻塞时继续有效，但不延续到后续修复快照。只有 P2 的 PASS 建议保持可见，不
   阻塞 Seal。

- `workflow show`：查看当前流程状态。
- `workflow resume`：从中断处继续，是备用路径；中断的派发保持 `PENDING`，已完成的结
  果不受影响。原生 HEAD 已漂移时，用 `--adopt-external --reason` 显式采纳外部改动。
- `workflow abort`：中止流程，保留中止摘要，清除临时目录。

## 接手与中途修改

**接手**（不同主代理接手中断的 run，或没有旧 run 的半途工作）= 新建 run（同一主代理续
跑自己的 run 用 `workflow resume`）。流程：构建/测试（明显失败先修）→ 提交未提交改动
（不纳入先暂存、seal 后恢复）→ 重走需求澄清（有已确认需求则重确认并登记）→ 按是否审
已有工作选基线：要审（未审过；中断 run 取其原始基线）→ `--base-snapshot <工作起始
点>`；信任或部分已审/拿不准 → 当前 HEAD（拿不准按要审处理）。需求 = "完成已有工作到
规范"或"在现有基础上继续 X"。

**中途修改**：任何项目文件改动都不自动作废结果。主代理逐结果判定——改动没触及该结果
→ 继承并记录理由；受影响或拿不准 → 独立判定或重跑。未选门/action 变化不阻塞（`resume`
报 `catalogDelta`）；外部 VCS 改动用 `resume --adopt-external` 重绑；需求修订用
`requirement --meaning preserved|changed` 分类。

## 独立派发

需求澄清由主代理直接执行。
继承判定也由主代理直接完成并记录理由，不需要单独准备继承判定提示词。
其他独立派发的动作或门，调用 `workflow prepare-action` 或 `prepare-gate`，把输出作为
完整任务通过 host 的独立代理通道发出。开发任务只发给开发工作者，任务内容固定为：已确认范围 + 实现任务；聊天历史、发现项、修复说明、其他审查者结论、预期判定一律留在编排层。

派发即原样转发：通过独立代理通道发出的任务消息就是 `prepare-action` / `prepare-gate` 的输出本身，主代理不得在任务前后附加评论、解释、先前结论、期望判定、修复说明或关注点。任务内容只由 CLI 组装；任何附加文本都不是任务的一部分，审查者应忽略。这是零上下文审查的软边界所在，派发纪律是第一道防线。

host 启动后，用 `workflow claim-dispatch` 把每个独立派发绑定到 host 身份。记录结果时
，或调用 `workflow snapshot --dispatch`（开发和修复用），会在改变状态前先检查生命周
期模块。总任务实例的集成快照由主代理操作，不加 `--dispatch`。

QA 审查和每个门审查必须创建全新的零上下文审查者，记录时原样传回派发 ID。同一流程实例
中每个 host 身份只能用一次；续用同一被中断派发保持已认领身份、不分配新身份。

被中断且已认领、未产出结果的派发，一律先恢复原代理继续完成同一派发；宿主无法恢复时再
重开新派发 + 新零上下文代理。

QA 审查者输入 = 已确认需求 + CLI 组装的完整候选用例集；不含生产代码、diff、测试、开
发者解释、其他审查者结论。门任务 = `prompts/reviewer-base.md` + `gates/<id>.md` + 需
求路线 + VCS 路线 + 结果契约，经 CLI 组装；提示词缺失即运行时错误中止派发。门审覆盖
完整"基线到当前"（重跑轮显式声明、不限于返修增量）；审查者报告 `compared` 快照对，
不匹配即丢弃。

运行时错误不是审查发现项，它们保持可见并需要重试或用户明确的跳过授权。状态询问只能询问进度，并且必须明确告知审查者继续，直到所有分配的检查全部完成或用户主动指示。

## 结果校验与修复上限

每份审查返回的结果只是候选输入，不是最终结论。主代理在记录或展示 PASS/FAIL 之前，必
须显式核查：需求是否匹配、是否在正常使用边界内、结果契约是否满足。对于 FAIL 或阻塞项
，还要核查流程状态、范围、严重度、因果关系和引用证据，并能从文档化的公开入口出发，通
过正常操作或常见失误端到端复现。任何一项核查不通过，就丢弃该发现项——不记录、不展示为
阻塞、不因此改变需求或实现。注意：这是编排层面的核查，不是又一道门或另一轮审查。

核查通过后，把同一根因的发现项归为一组，在同一个修复轮次中一并处理所有 P0、P1 和 P2
。

**审查轮次上限：** QA 和所有已选门共享最多三次自动审查轮次（整个交付尝试内）。计入规
则：开发后首轮审查计一次，每个修复后的审查轮次计一次，但须所有必需验证都完成才算，且
每个快照最多计一次；派发失败、中断、缺少验证、PENDING、RUNTIME_ERROR 均不计入。上限
用尽后，每次额外修复前都要向用户展示当前阻塞项并请求授权。每次授权只允许一轮修复/审
查，不能累积；那轮仍有阻塞时，重复这个请求过程。用户也可以选择授权 Seal 跳过，或授权
一次需求变更。

## 门文件

开发后审查门就是已安装包的 `gates/` 目录下的直接 Markdown 子文件。文件名（不含扩展名
）即门 ID，按字典序排列。`gates/qa.md` 无效，`qa` 是内置 QA 流程的保留名称。要新增门
，加一个有效文件并重新安装；要移除门，删文件并重新安装。不要添加注册表、manifest、
front matter、权重、依赖图或任何项目级配置。

QA 设计、QA 审查、QA 执行、需求澄清、产品审、开发前技术检查、继承判定、开发和 Seal
都是流程动作，不是门文件。

## 状态与 VCS

流程的唯一临时状态文件是 `.gates/tmp/<run-id>/state.json`，不要手工编辑。CLI 负责管
理：原子更新、提示词/目录修订、动作和门的状态、已批准用例、当前快照绑定。CLI 不存储
：diff 内容、项目文件、证据图、分层产物或第二套版本控制数据。

CLI 使用流程启动时选定的 Git、SVN 或 P4 原生命令解析和验证快照标识；host 和各代理都
直接用同一个 VCS 做比较。"基线到当前"是完整交付 diff，新执行和重跑的门都用它；"修复
前紧邻快照到当前"是修复 diff，仅供继承判定使用，在提示词里与门重跑范围明确区分。门审
查者必须报告它实际比较的快照对。VCS 无法复现不可变比较时，记录 `RUNTIME_ERROR`，不
要猜测、不要偷偷切换 VCS、不要自己实现 diff 回退。具体 VCS 操作见
`references/vcs-snapshots.md`。

## 安装与维护

处理安装、host hook、canary、打包或发布检查时，再去读
`references/install-and-hooks.md`。维护本仓库时，再去读
`references/local-validation.md`。hook 配置只是配置，不等于 hook 已经运行；必须在同
一个 host 上通过实机 canary 验证，才能认定 hook 阻塞已生效。
