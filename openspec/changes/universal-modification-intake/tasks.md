# Tasks

- [x] Activate the skill for every modification request and make outcome plus
  consequential-solution clarification a hard pre-write requirement.
- [x] Require a complete consolidated requirements/solution summary and explicit
  confirmation immediately before writes.
- [x] Combine lightweight, full, and custom routing into one recommendation and
  user choice using stateless dynamic candidates.
- [x] Preserve lightweight access to optional PRD/OpenSpec/planning artifacts.
- [x] Require a durable format-neutral requirements and solution document before
  full or custom development without requiring a document plugin.
- [x] Preserve route choice across added requirements and require renewed summary
  confirmation without another route question.
- [x] Require judgment-based task slicing and independent formal runs per slice.
- [x] Start and retain one overall formal run before sliced development; run
  independent formal slices concurrently in separate VCS worktrees; after
  merging and conflict resolution, record the merged snapshot and run
  integration QA and gates in that same overall run; return findings to slice
  runs and record merged slice repairs without an overall development worker.
- [x] Remove `none` route code, tests, and every public description.
- [x] Remove repository-specialized intake and maintenance branches.
- [x] Integrate small-repair Carry/QA inheritance guidance.
- [x] Require the main agent to validate every candidate blocker against the
  confirmed requirement and independently reproduce its normal public path and
  evidence before recording or reporting it; discard unsupported findings.
- [x] Align SKILL, READMEs, agent metadata, changelog, and behavior examples.
- [x] Run the complete package validation checklist and selected formal gates.
