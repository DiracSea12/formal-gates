# 项目代理规则

这是一个个人项目。只保证文档化需求功能的正常使用和常见操作失误。除非用户明确要求加固，对抗性输入、恶意编辑、手工改写状态、权限或不可变文件失败、不受支持的平台，以及其他人为构造的攻击或违规，都只作 P2 建议，不能阻塞 PASS，也不能引出新机制、兼容路径或测试。

发现项严重度 P0/P1/P2 的定义与复现路径规则见 `prompts/reviewer-base.md`。

在每一次子代理派发中都明确重申这条边界。范围外的发现项仅作建议；如果不存在范围内的阻塞项，结果必须为 PASS。不要为异常修改添加防御、兼容路径、测试或流程步骤。

QA 用例设计与执行使用同一条项目边界。在直接拥有该行为的最低层测试每个行为。当既有的直接自动化覆盖已经验证同一条确定性规则时，缺少更高层的重复覆盖仅作建议；只有当更高层引入了低层无法覆盖的正常使用行为时，它才可以阻塞。不要要求那些主要目的是重测其他测试或校验器的测试。除非用户明确要求这类覆盖，不要添加对抗性、改写内部状态、权限、不可变文件或不受支持平台的用例。

每个阻塞项都必须包含从文档化公开入口出发、且只使用正常用户操作或常见失误的端到端复现。需要手工创建或改写内部状态、临时运行数据或攻击式输入的复现仅作建议。在报告某项改动无法推送之前，主代理必须独立验证所声称的正常使用复现路径。

绝不催促或施压审查子代理。不要让它停止阅读或检查、提前收敛、只使用已收集的证据、立即返回，或以其他方式缩短分配给它的审查。状态询问只能询问进度，并且必须明确告诉子代理继续，直到每一项分配的检查全部完成。在不当施压之后返回的判定无效，必须丢弃并干净重跑，且不计入一次已完成的审查-修复循环。
