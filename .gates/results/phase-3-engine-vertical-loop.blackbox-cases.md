# Blackbox QA cases (run: phase-3-engine-vertical-loop)

Derived from the run state at qa-design record time. Review these cases against the current confirmed requirement.

CASE-001
mode: blackbox
description: 已登记稳定 launcher 启动 candidate engine，active/terminal 隔离 stable namespace 且终结 cleanup intent 不残留
procedure: FIXTURE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/scripts/phase3-qa-fixture.sh；SOURCE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor；在独立 D/P/H/R 执行 prepare，随后立即 CAND_BIN=$(jq -r .candidate D/prepare.json)，并仅用该稳定 launcher 执行现有 start、active show/status/next/diagnose、首次 drive、terminal show/status/next/diagnose 及 drive/next replay。首次 drive 前记录 P/.gates/engine/c001-engine 下的 state/request/cleanup.intent/cleanup.receipt 清单、SHA-256、mtime、revision；首次 drive 后、terminal replay 后分别执行 test ! -e P/.gates/engine/c001-engine/cleanup.intent.json，读取 cleanup.receipt.json，并递归扫描该 run 目录确认不存在 cleanup.intent.json、UNKNOWN 或未回收状态；同时复采 engine/tmp/resource/launcher/registry 快照。
oracle: 既有 start/active/terminal/replay 成功、engine/lightweight 绑定、COMPLETE/unverified=true、envelope/receipt/canonical artifact 与 namespace oracle 全部保持；首次 drive 前无 cleanup.intent；首次 drive 完成后 cleanup.intent.json 必须不存在，cleanup.receipt.json 必须为 status==reconciled 且 residue==false，engine run 目录不得残留 cleanup.intent、UNKNOWN 或未完成 cleanup marker；terminal show/next/drive replay rc=0 且 bytes、mtime、revision 不变，stable launcher/registry 不变.
review status: PASS

CASE-002
mode: blackbox
description: candidate lightweight start 对真实空内容、非法 JSON 及不存在路径的 intake receipt 均在首写前返回稳定 typed 错误，不泄漏底层文件系统细节且不回退 legacy
procedure: 建立三个独立 fixture。每个 fixture 执行 FIXTURE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/scripts/phase3-qa-fixture.sh prepare --project-root P --home H --source /Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor --candidate /Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/bin/formal-gates --receipt R，随后立即 CAND_BIN=$(jq -r .candidate prepare.json)，读取 P/PKG。A 在 D/empty-object.json 写入字节 {}，确认文件存在且非零，再执行 env HOME=H USERPROFILE=H CODEX_HOME= CAND_BIN workflow start --root P --package-root PKG --run-id c002-empty-object --route lightweight --intake-receipt D/empty-object.json。B 在 D/invalid.json 写入字节 {，确认文件存在，再执行同一 candidate start，run-id=c002-invalid-json，--intake-receipt D/invalid.json。C 确认 D/not-found.json 不存在，再执行同一 candidate start，run-id=c002-no-path，附带 --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt D/not-found.json。保存每条命令的完整 argv/stdout/stderr/rc，并在命令前后保存 engine/tmp/resource/registry/receipt 清单、字节、SHA-256、mtime。
oracle: A、B、C 均 rc=1 且含 INVALID_INTAKE_CONFIRMATION。A 必须是缺少 typed receipt 内容的稳定 schema/binding 错误，不得出现 JSON parser、legacy fallback、open/read、no such file 或任何 D/P/H 绝对路径。B 错误必须精确为 INVALID_INTAKE_CONFIRMATION: intakeConfirmationReceipt JSON is invalid，不得附加底层 parser 或文件路径。C 错误必须精确为 INVALID_INTAKE_CONFIRMATION: intake receipt is unavailable，不得附加 open/read、no such file 或文件路径。三条均不得创建 .gates/tmp/<run> 或 engine run，candidate/stable/registry/receipt 观察根逐字节、SHA-256、mtime 不变。
review status: PASS

CASE-003
mode: blackbox
description: intake confirmation 八个独立失配变体首写前可观察拒绝，authority/transport 必须是 typed 字段
procedure: 对 source、requirementRevision、solutionRevision、solutionDigest、artifactRevision、artifactSet、authority、transport 各建独立 fixture；prepare 后立即读取 CAND_BIN=$(jq -r '.candidate' prepare.json)，fixture mutate 生成 BAD，并用 CAND_BIN start --intake-receipt BAD；typed 六项分别确认对应字段变更，authority/transport 先 jq -e has('authority') and has('transport')，保存各自 rc/error 与 engine/tmp/resource/registry 前后快照。
oracle: 六个当前 typed 失配均 rc=1 且含 INVALID_INTAKE_CONFIRMATION、无任何写入；authority/transport 不得被未知 JSON 字段吞掉，必须把 receipt 公共 shape 扩展为 typed authority/transport（固定 launcher 值，变体写 unregistered-authority/non-fixed-transport）并在 candidate 校验中 rc=1/零写入；落地前这两项是 schema 覆盖缺口，不得记 PASS。
review status: PASS

CASE-004
mode: blackbox
description: ConfirmedAt 很旧但绑定一致仍可运行，无 wall-clock expiry
procedure: fixture prepare --old-confirmed-at 到独立 D/P/H/R，紧随执行 CAND_BIN=$(jq -r '.candidate' prepare.json)，读取 PKG/R 并 jq 确认 confirmedAt=2000-01-01T00:00:00Z；用 CAND_BIN start --intake-receipt R、首次 drive、active/terminal show/status/next/diagnose 与 drive/next replay，保存 summary/receipt/namespace 快照。
oracle: start/drive/读取/replay 均 rc=0；不因时间拒绝，engine/c004-old 终态 COMPLETE/unverified=true；一份 receipt、digest/绑定一致、自动 cleanup、replay 只读，stable/registry 无变化。
review status: PASS

CASE-005
mode: blackbox
description: admission registry 缺失、未注册、冲突均拒绝
procedure: A prepare 后换全新空 HOME start；B 用 HOME2 的登记和 P1/P2 交叉组合 start 制造 root/target mismatch；C 同 HOME/CAND_BIN 对两个项目 prepare 后第三个项目省略 package-root 触发多条 active registry record。保存命令前后快照。
oracle: A/B/C 均 rc=1 且含 UNREGISTERED_INSTALL；无任何 state/registry 写入。
review status: PASS

CASE-006
mode: blackbox
description: 禁止用户覆盖 engine、runtime、identity、digest
procedure: 四个独立 fixture 的完整 candidate start 分别追加 --engine stable、--runtime legacy、--identity wrong-target-id、--digest sha256:加64个0；保存 rc/output 与快照。
oracle: 四项均是 flag provided but not defined: -engine/-runtime/-identity/-digest；零写入。
review status: PASS

CASE-007
mode: blackbox
description: 候选启动拒绝旧控制参数
procedure: 三个独立 fixture 的完整 candidate start 分别追加 --split no、--retained-overall、--master retained-overall-fixture；保存 rc/output 与快照。
oracle: 三项 rc=1，含 candidate engine start does not accept --split, --retained-overall, or --master；零写入。
review status: PASS

CASE-008
mode: blackbox
description: ACTIVE engine run 的 writer/六项 envelope 屏障失配拒绝且 diagnose 只读
procedure: 对 writer、stateSchemaVersion、workflowDefinitionVersion、definitionSource、definitionDigest、packageDigest、installedTargetIdentity 各建独立 fixture；prepare 后立即读取 CAND_BIN=$(jq -r '.candidate' prepare.json)，用 CAND_BIN start 建 ACTIVE；fixture mutate-envelope --input-project P --output-project D/mismatch-project --run-id RUN --field FIELD，读取 runRoot/mutatedPath，记录 clone 快照；在 clone 用 CAND_BIN drive --root M --run-id RUN 后 diagnose --root M --run-id RUN。
oracle: 每项 drive rc=1 且含 UNSUPPORTED_RUN_VERSION 与字段（writer 也必须识别），clone state/intent/receipt/resource/registry bytes/sha256/mtime/revision 零变化；diagnose rc=0，jsonReadable=true、integrity=unsupported、detectedVersions/mismatch/recommendation 含 UNSUPPORTED_RUN_VERSION，且 diagnose 只读。
review status: PASS

CASE-009
mode: blackbox
description: 同一 ACTIVE candidate run 的完整九个旧写入口统一 fence，随后 drive 仍完成
procedure: prepare 后立即用 jq 取 CAND_BIN，完整 start RUN=c009-engine 保持 ACTIVE；固定同一 run 依次执行 requirement --source requirements.md --requirement-artifact design.md --confirmed、cleanup --run c009-engine、resume、abort --user-confirm、reset --user-approve、route --mode full、route-add --gate blackbox-qa、slicing --decision no-split --note 'engine fence probe'、settle-findings --action product-review --dismiss 'fixture finding'，每条前后记录 engine/resource/intent/receipt/stable/registry 清单和 sha256/mtime，最后合法 drive。
oracle: 九条均 rc=1 含 UNSUPPORTED_ENGINE_ENTRY 且非 parser error，run 始终 ACTIVE、快照不变；最终 drive rc=0，show/next 为 engine/lightweight/COMPLETE/unverified=true，stable 无写入。
review status: PASS

CASE-010
mode: blackbox
description: 反向 artifact 输入也必须 canonical 持久化、intakeDigest 相同且不重复确认
procedure: 独立 A/B prepare；A prepare 紧随取 CAND_BIN_A=$(jq -r '.candidate' DA/prepare.json)，B prepare 紧随取 CAND_BIN_B=$(jq -r '.candidate' DB/prepare.json)，各自 workflow 命令始终使用对应稳定 launcher。B 对 receipt 执行 fixture mutate --field artifactSet，再 jq '.artifacts |= .[0:2]' 生成仅反向两个 artifact 的 receipt，并用 jq 机械确认 path 列表不等于 sort。A/B 用各自稳定 launcher start、首次 drive、terminal show/next、drive/next replay，保存 intakeReceipt、state、summary、receipt 数和 bytes/mtime。
oracle: A/B start/drive rc=0，终态 COMPLETE/unverified=true；持久 confirmation.artifacts 和 state/summary path 列表严格等于 LC_ALL=C sort，B 不保留反向顺序；intakeDigest 相同、绑定/solutionDigest 相同；每 run 一份 receipt/一次确认，无 UNKNOWN/跨 namespace，replay bytes/revision/mtime 不变。
review status: PASS

CASE-011
mode: blackbox
description: stable launcher legacy 回归公开命令集合保持旧语义并与 candidate 隔离
procedure: prepare 独立 fixture 后立即取 CAND_BIN=$(jq -r '.candidate' prepare.json)，记录 tmp/engine/launcher/registry。用 CAND_BIN 执行 legacy start --run-id c011-legacy --requirement requirements.md --requirement-artifact design.md --vcs git --split no；prepare-action requirements-clarification，从 state.json 取唯一 DISPATCH，record-action PASS，requirement --source requirements.md --requirement-artifact design.md --confirmed；再 show、diagnose、status、next，cleanup --run c011-legacy，并 cleanup --run phase3-fixture-legacy，保存 cleanup JSON 和终态快照。
oracle: legacy 命令均按 oracle 成功/拒绝：run 在 .gates/tmp 无 engine envelope，show rc=0；diagnose rc=0 jsonReadable=true integrity unsupported 且 recommendation 含 UNSUPPORTED_RUN_VERSION: state has no version envelope，前后只读；status/next rc=1 且含 candidate-engine-only 精确错误；cleanup deleted=true 且 temp 消失；candidate engine、launcher、registry 全程不变.
review status: PASS

CASE-018
mode: blackbox
description: candidate 与 legacy 对同一 run-id 的活动状态及 retained result 互斥，禁止跨 runtime 创建、续跑或双写
procedure: 定义 FIXTURE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/scripts/phase3-qa-fixture.sh、SOURCE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor、CANDIDATE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/bin/formal-gates、ROOT=$(mktemp -d /tmp/phase3-case018.XXXXXX)。定义 D_A=$ROOT/A/D、P_A=$ROOT/A/P、H_A=$ROOT/A/H、R_A=$ROOT/A/R.json；D_B=$ROOT/B/D、P_B=$ROOT/B/P、H_B=$ROOT/B/H、R_B=$ROOT/B/R.json；D_C=$ROOT/C/D、P_C=$ROOT/C/P、H_C=$ROOT/C/H、R_C=$ROOT/C/R.json；D_D=$ROOT/D/D、P_D=$ROOT/D/P、H_D=$ROOT/D/H、R_D=$ROOT/D/R.json。执行 mkdir -p "$D_A" "$D_B"；再执行 mkdir -p "$D_C" "$D_D"。A 执行 "$FIXTURE" prepare --project-root "$P_A" --home "$H_A" --source "$SOURCE" --candidate "$CANDIDATE" --receipt "$R_A" > "$D_A/prepare.json" 2> "$D_A/prepare.stderr"；执行 CAND_BIN_A=$(jq -r '.candidate' "$D_A/prepare.json") 和 PKG_A=$(jq -r '.packageRoot' "$D_A/prepare.json")。B 执行 "$FIXTURE" prepare --project-root "$P_B" --home "$H_B" --source "$SOURCE" --candidate "$CANDIDATE" --receipt "$R_B" > "$D_B/prepare.json" 2> "$D_B/prepare.stderr"；执行 CAND_BIN_B=$(jq -r '.candidate' "$D_B/prepare.json") 和 PKG_B=$(jq -r '.packageRoot' "$D_B/prepare.json")。C 执行 "$FIXTURE" prepare --project-root "$P_C" --home "$H_C" --source "$SOURCE" --candidate "$CANDIDATE" --receipt "$R_C" > "$D_C/prepare.json" 2> "$D_C/prepare.stderr"；执行 CAND_BIN_C=$(jq -r '.candidate' "$D_C/prepare.json") 和 PKG_C=$(jq -r '.packageRoot' "$D_C/prepare.json")。D 执行 "$FIXTURE" prepare --project-root "$P_D" --home "$H_D" --source "$SOURCE" --candidate "$CANDIDATE" --receipt "$R_D" > "$D_D/prepare.json" 2> "$D_D/prepare.stderr"；执行 CAND_BIN_D=$(jq -r '.candidate' "$D_D/prepare.json") 和 PKG_D=$(jq -r '.packageRoot' "$D_D/prepare.json")。A 执行 env HOME="$H_A" USERPROFILE="$H_A" CODEX_HOME= "$CAND_BIN_A" workflow start --root "$P_A" --package-root "$PKG_A" --run-id c012-legacy-active --requirement requirements.md --requirement-artifact design.md --vcs git --split no > "$D_A/legacy-start.stdout" 2> "$D_A/legacy-start.stderr"；保存 rc 到 "$D_A/legacy-start.rc"；记录 "$P_A/.gates/tmp/c012-legacy-active/state.json" 的 bytes、SHA-256、mtime、revision；再执行 env HOME="$H_A" USERPROFILE="$H_A" CODEX_HOME= "$CAND_BIN_A" workflow start --root "$P_A" --package-root "$PKG_A" --run-id c012-legacy-active --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt "$R_A" > "$D_A/candidate-start.stdout" 2> "$D_A/candidate-start.stderr"；保存 rc 到 "$D_A/candidate-start.rc"；检查 "$P_A/.gates/engine/c012-legacy-active" 不存在并再次记录 legacy state 快照。B 执行 env HOME="$H_B" USERPROFILE="$H_B" CODEX_HOME= "$CAND_BIN_B" workflow start --root "$P_B" --package-root "$PKG_B" --run-id c012-engine-active --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt "$R_B" > "$D_B/candidate-start.stdout" 2> "$D_B/candidate-start.stderr"；保存 rc 到 "$D_B/candidate-start.rc"；记录 "$P_B/.gates/engine/c012-engine-active/state.json" 和 request 的 bytes、SHA-256、mtime、revision；再执行 env HOME="$H_B" USERPROFILE="$H_B" CODEX_HOME= "$CAND_BIN_B" workflow start --root "$P_B" --package-root "$PKG_B" --run-id c012-engine-active --requirement requirements.md --requirement-artifact design.md --vcs git --split no > "$D_B/legacy-start.stdout" 2> "$D_B/legacy-start.stderr"；保存 rc 到 "$D_B/legacy-start.rc"；检查 "$P_B/.gates/tmp/c012-engine-active" 不存在并再次记录 candidate state/request 快照。C 执行 env HOME="$H_C" USERPROFILE="$H_C" CODEX_HOME= "$CAND_BIN_C" workflow abort --root "$P_C" --run-id phase3-fixture-legacy --user-confirm > "$D_C/abort.stdout" 2> "$D_C/abort.stderr"；保存 rc 到 "$D_C/abort.rc"；执行 jq -e '.status == "ABORTED"' "$D_C/abort.stdout"；执行 test ! -e "$P_C/.gates/tmp/phase3-fixture-legacy"；定义 RETAINED_C="$P_C/.gates/results/phase3-fixture-legacy.json"，执行 test -f "$RETAINED_C"，并将 shasum -a 256 "$RETAINED_C" 的结果保存到 "$D_C/retained-before.sha256"，将 stat -f '%m %z' "$RETAINED_C" 的结果保存到 "$D_C/retained-before.stat"；再执行 env HOME="$H_C" USERPROFILE="$H_C" CODEX_HOME= "$CAND_BIN_C" workflow start --root "$P_C" --package-root "$PKG_C" --run-id phase3-fixture-legacy --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt "$R_C" > "$D_C/candidate-start.stdout" 2> "$D_C/candidate-start.stderr"；保存 rc 到 "$D_C/candidate-start.rc"；再次执行 shasum -a 256 "$RETAINED_C" > "$D_C/retained-after.sha256" 和 stat -f '%m %z' "$RETAINED_C" > "$D_C/retained-after.stat"，并记录 "$P_C/.gates/engine/phase3-fixture-legacy" 是否存在。D 执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow start --root "$P_D" --package-root "$PKG_D" --run-id c012-engine-retained --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt "$R_D" > "$D_D/candidate-start.stdout" 2> "$D_D/candidate-start.stderr"；保存 rc 到 "$D_D/candidate-start.rc"；执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow drive --root "$P_D" --run-id c012-engine-retained > "$D_D/drive.stdout" 2> "$D_D/drive.stderr"；保存 rc 到 "$D_D/drive.rc"；执行 jq -e '.status == "COMPLETE"' "$D_D/drive.stdout"；定义 TERMINAL_D="$P_D/.gates/engine/c012-engine-retained/terminal-summary.json"；分别执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow show --root "$P_D" --run-id c012-engine-retained > "$D_D/show.stdout" 2> "$D_D/show.stderr"，保存 rc 到 "$D_D/show.rc"；执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow status --root "$P_D" --run-id c012-engine-retained > "$D_D/status.stdout" 2> "$D_D/status.stderr"，保存 rc 到 "$D_D/status.rc"；执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow next --root "$P_D" --run-id c012-engine-retained > "$D_D/next.stdout" 2> "$D_D/next.stderr"，保存 rc 到 "$D_D/next.rc"；执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow diagnose --root "$P_D" --run-id c012-engine-retained > "$D_D/diagnose.stdout" 2> "$D_D/diagnose.stderr"，保存 rc 到 "$D_D/diagnose.rc"；执行 shasum -a 256 "$TERMINAL_D" > "$D_D/terminal-before.sha256" 和 stat -f '%m %z' "$TERMINAL_D" > "$D_D/terminal-before.stat"；再执行 env HOME="$H_D" USERPROFILE="$H_D" CODEX_HOME= "$CAND_BIN_D" workflow start --root "$P_D" --package-root "$PKG_D" --run-id c012-engine-retained --requirement requirements.md --requirement-artifact design.md --vcs git --split no > "$D_D/legacy-start.stdout" 2> "$D_D/legacy-start.stderr"；保存 rc 到 "$D_D/legacy-start.rc"；执行 shasum -a 256 "$TERMINAL_D" > "$D_D/terminal-after.sha256" 和 stat -f '%m %z' "$TERMINAL_D" > "$D_D/terminal-after.stat"，并记录 "$P_D/.gates/tmp/c012-engine-retained" 是否存在。
oracle: A 的 candidate start 必须 rc=1 且精确含 run "c012-legacy-active" already exists in legacy runtime；不得创建 .gates/engine/c012-legacy-active，legacy state 的 bytes、SHA-256、mtime、revision 不变。B 的 legacy start 必须 rc=1 且精确含 run "c012-engine-active" already exists in candidate runtime；不得创建 .gates/tmp/c012-engine-active，candidate state/request 的 bytes、SHA-256、mtime、revision 不变。C 的 abort 必须 rc=0，stdout 必须为 JSON 且 status=="ABORTED"，不得出现 parser 错误；P_C/.gates/tmp/phase3-fixture-legacy 必须不存在，RETAINED_C 必须存在，retained result 的 before/after SHA-256 和 mtime/size 必须一致；随后 candidate start 必须 rc=1 且精确含 run "phase3-fixture-legacy" already has a retained legacy result，不得创建 engine namespace。D 的 candidate drive 必须 rc=0 且 stdout status=="COMPLETE"；show、status、next、diagnose 必须成功并指向 candidate engine run，TERMINAL_D 在 legacy start 前后 SHA-256 和 mtime/size 必须一致；随后 legacy start 必须 rc=1 且精确含 run "c012-engine-retained" already exists in candidate runtime，不得创建 legacy namespace。所有拒绝均须发生在目标 runtime 首次写入前，不得以 INVALID_INTAKE_CONFIRMATION 或 UNREGISTERED_INSTALL 代替 run-id 互斥，不得产生第二 writer、跨 runtime handoff、UNKNOWN 或额外结果。
review status: PASS

CASE-019
mode: blackbox
description: 清理 fixture 遗留的 legacy ACTIVE run 后，目标 ACTIVE candidate engine run 对文档化 install --force 与 uninstall 维护入口实施 stable launcher fencing
procedure: 定义 FIXTURE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/scripts/phase3-qa-fixture.sh、SOURCE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor、CANDIDATE=/Users/suxinyang/Documents/Codex/2026-06-28/nin/work/formal-gates-refactor/bin/formal-gates、ROOT=$(mktemp -d /tmp/phase3-case019.XXXXXX)。定义 D_A=$ROOT/A/D、P_A=$ROOT/A/P、H_A=$ROOT/A/H、R_A=$ROOT/A/R.json、D_B=$ROOT/B/D、P_B=$ROOT/B/P、H_B=$ROOT/B/H、R_B=$ROOT/B/R.json，并创建 D_A、D_B。对 A 执行 "$FIXTURE" prepare --project-root "$P_A" --home "$H_A" --source "$SOURCE" --candidate "$CANDIDATE" --receipt "$R_A" > "$D_A/prepare.json" 2> "$D_A/prepare.stderr"，随后执行 CAND_BIN_A=$(jq -r '.candidate' "$D_A/prepare.json") 和 PKG_A=$(jq -r '.packageRoot' "$D_A/prepare.json")。对 B 执行 "$FIXTURE" prepare --project-root "$P_B" --home "$H_B" --source "$SOURCE" --candidate "$CANDIDATE" --receipt "$R_B" > "$D_B/prepare.json" 2> "$D_B/prepare.stderr"，随后执行 CAND_BIN_B=$(jq -r '.candidate' "$D_B/prepare.json") 和 PKG_B=$(jq -r '.packageRoot' "$D_B/prepare.json")。A 先执行 env HOME="$H_A" USERPROFILE="$H_A" CODEX_HOME= "$CAND_BIN_A" workflow abort --root "$P_A" --run-id phase3-fixture-legacy --user-confirm，保存 stdout、stderr、rc；用 jq -e '.status == "ABORTED"' "$D_A/abort.stdout" 检查摘要状态，用 test ! -e "$P_A/.gates/tmp/phase3-fixture-legacy" 检查临时目录已删除，并记录清理后的 registry/tmp 快照；再执行 env HOME="$H_A" USERPROFILE="$H_A" CODEX_HOME= "$CAND_BIN_A" workflow start --root "$P_A" --package-root "$PKG_A" --run-id c013-install-active --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt "$R_A"，保存 stdout、stderr、rc 和 active state、request、intent、receipt、summary、resource、tmp、stable launcher、package target、registry、hook/config、recovery journal 的路径清单、bytes、SHA-256、mode、size、mtime、revision；然后执行 env HOME="$H_A" USERPROFILE="$H_A" CODEX_HOME= "$CAND_BIN_A" install --source "$H_A/phase3-source" --host codex --scope project --project "$P_A" --binary-target "$CAND_BIN_A" --skip-hooks --force，保存完整 stdout、stderr、rc 和同一组快照。B 先执行 env HOME="$H_B" USERPROFILE="$H_B" CODEX_HOME= "$CAND_BIN_B" workflow abort --root "$P_B" --run-id phase3-fixture-legacy --user-confirm，保存 stdout、stderr、rc；用 jq -e '.status == "ABORTED"' "$D_B/abort.stdout" 检查摘要状态，用 test ! -e "$P_B/.gates/tmp/phase3-fixture-legacy" 检查临时目录已删除，并记录清理后的 registry/tmp 快照；再执行 env HOME="$H_B" USERPROFILE="$H_B" CODEX_HOME= "$CAND_BIN_B" workflow start --root "$P_B" --package-root "$PKG_B" --run-id c013-uninstall-active --requirement requirements.md --requirement-artifact design.md --vcs git --route lightweight --intake-receipt "$R_B"，保存 stdout、stderr、rc 和同一组快照；然后执行 env HOME="$H_B" USERPROFILE="$H_B" CODEX_HOME= "$CAND_BIN_B" uninstall --host codex --scope project --project "$P_B"，保存完整 stdout、stderr、rc 和同一组快照。
oracle: A、B 的 abort 均必须 rc=0，stdout 必须为 JSON 且 status=="ABORTED"，不得报 parser 错误；清理后不得再有 phase3-fixture-legacy ACTIVE state，且对应 .gates/tmp 目录必须不存在。A 的 install --force 必须 rc=1，stderr 必须包含 active workflow run "c013-install-active" at 以及实际 P_A/.gates/engine/c013-install-active/state.json 路径和 fences install；B 的 uninstall 必须 rc=1，stderr 必须包含 active workflow run "c013-uninstall-active" at 以及实际 P_B/.gates/engine/c013-uninstall-active/state.json 路径和 fences uninstall。两条维护命令均不得替换或删除 installed target、stable launcher、hook/config、registry record 或 recovery journal，不得改变目标 active engine state 的 bytes、SHA-256、mtime、revision，不得写 disabled/partial registry 或删除目标 run；失败必须归因于目标 c013 active run 的 stable fencing，而非遗留 run、parser、权限或未登记错误。
review status: PASS

